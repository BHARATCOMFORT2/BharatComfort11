[build]
  command = "npm run build"
  publish = ".next"
  functions = "netlify/functions/"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["razorpay", "@netlify/next"]

[[plugins]]
  package = "@netlify/next"

[build.environment]
  NEXT_DISABLE_ENV_INJECTION = "true"
