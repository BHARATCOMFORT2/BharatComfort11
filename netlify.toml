[build]
  command = "npm run build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["razorpay"]

# Redirect all unmatched routes to Next.js
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
