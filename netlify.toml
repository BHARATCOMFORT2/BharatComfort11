[build]
  command = "npm run build"
  publish = ".next"

# Ensure Next.js runs in Netlify’s runtime
[functions]
  node_bundler = "esbuild"

# Redirects (optional, for clean routes)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
